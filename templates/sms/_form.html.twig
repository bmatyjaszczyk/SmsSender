{{ form_start(form) }}

    {{ form_errors(form) }}

    {{ form_row(form.recipient, {label: 'Recipent (with country code)', 'attr': {'class': 'form-control num-only', 'placeholder': '+441234567891', 'pattern': '^[+][0-9]{1,3}[0-9]{5,13}$'}}) }}
    {{ form_row(form.body, {label: 'Body', type: 'textarea', 'attr': {'class': 'form-control', 'maxlength': 140}}) }}
    {{ form_rest(form) }}
    <div class="mt-3 mb-3">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>

{{ form_end(form) }}
